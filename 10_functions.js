function calculateCartPrice(va1, val2,...num){   // spread operator or rest operator
    return num
}

console.log(calculateCartPrice(23,24,25))

const user = {
    username: "Ritik",
    price: 199
}

function handleObject(anyObject){
    console.log(`Username is ${user.username} and price is ${user.price}`)
}

handleObject(user)